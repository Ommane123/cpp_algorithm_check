#include <iostream>
#include <vector>
using namespace std;

int insertionSort(vector<int> &v)
{
    int n = v.size();
    for (int i = 1; i < n; i++)
    {
        int current_index = i;
        for (int j = i + 1; j < n; j++)
        {
        }
    }
}

int main()
{

    return;
}